# Pragmatic lint config for the embedded dashboard.
# Default safe rules: pyflakes (F) + select pycodestyle (E4/E7/E9).
# Intentional try/except in a serial driver is excluded via ignore set.
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F"]
ignore = ["E701"]
