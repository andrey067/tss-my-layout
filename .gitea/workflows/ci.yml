name: ci

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    container:
      image: node:22
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Python
        run: |
          apt-get update -qq
          apt-get install -y -qq python3 python3-pip python3-venv
          python3 --version

      - name: Setup venv
        run: python3 -m venv /tmp/venv

      - name: Install dependencies
        run: |
          /tmp/venv/bin/pip install --no-cache-dir -r requirements.txt ruff

      - name: Syntax check
        run: /tmp/venv/bin/python -m py_compile *.py tests/*.py

      - name: Lint
        run: /tmp/venv/bin/ruff check .

      - name: Smoke test
        run: /tmp/venv/bin/python -m unittest tests/smoke_test.py -v
